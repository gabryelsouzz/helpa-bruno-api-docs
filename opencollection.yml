opencollection: 1.0.0

info:
  name: HELPA
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{apiKey}}"
  variables:
    - name: baseUrl
      value: http://localhost:3333/
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
